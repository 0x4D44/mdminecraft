# Code Review Fixes Journal
Date: 2026-01-08

## Goals
- Fix multiplayer handshake response path.
- Fix TLS hostname/SNI handling in client connect.
- Make persistence writes atomic and durable.
- Prune disconnected clients on send failure.
- Reduce QUIC stream churn for reliable messages.

## TDD Log
- 00: Init journal.
- 01: Added failing tests for multiplayer handshake + prune, TLS server name resolution, atomic write cleanup, and multi-frame reliable stream handling.
- 02: Implemented handshake response, client pruning on send failure, TLS server-name resolution, atomic write helper in persistence, and reliable stream inbox + send stream reuse.

## 03 — Tests
- 2026-01-08: Ran `cargo test -p mdminecraft-world --lib` (692 passed).
